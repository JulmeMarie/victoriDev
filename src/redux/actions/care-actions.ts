import { PayloadAction } from "@reduxjs/toolkit";

export const careActions = {

    selectCare: (state: any, action: PayloadAction) => {

    },

    createCare: (state: any, action: PayloadAction) => {

    },

    updateCare: (state: any, action: PayloadAction) => {

    },

    deleteCare: (state: any, action: PayloadAction) => {

    },
}