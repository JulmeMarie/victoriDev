import { PayloadAction } from "@reduxjs/toolkit";

export const notificationActions = {

    isSeen: (state: any, action: PayloadAction) => {

    },
    setNew: (state: any, action: PayloadAction) => {

    },
    setAll: (state: any, action: PayloadAction) => {

    },

}